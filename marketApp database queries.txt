CREATE TABLE IF NOT EXISTS ITEM(
id identity,
name varchar(50) not null,
type varchar(50) not null,
quantity integer,
price number
);

INSERT INTO ITEM(NAME, TYPE, QUANTITY, PRICE)VALUES
('Apple', 'Food', 2, 22.5),
('Banana',  'Food', 1 , 10);


CREATE TABLE IF NOT EXISTS USER(
id identity, 
username varchar(50) not null,
password varchar(255) not null
);

INSERT INTO USER (USERNAME, PASSWORD)VALUES
('admin', 'MTIz');

CREATE TABLE IF NOT EXISTS USER_ITEM(
user_id bigint not null,
item_id bigint not null,
constraint fk_user foreign key (user_id) references user(id),
constraint fk_item foreign key (item_id) references item(id)
);

INSERT INTO USER_ITEM(USER_ID, ITEM_ID) VALUES
(1, 1);
(1, 2);


///QUERY TO FETCH ITEM FOR USER ID
SELECT ITEM.* FROM ITEM INNER JOIN
USER_ITEM ON ITEM.ID = ITEM_ID INNER JOIN
USER ON USER_ID = USER.ID
WHERE USER.ID = ?